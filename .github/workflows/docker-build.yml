name: Build & Push Docker Image

on:
  workflow_dispatch:
  push:
    branches: [master]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ceesam111
          password: ${{ secrets.CR_PAT }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          build-args: |
            NEXT_PUBLIC_SUPABASE_URL=https://rnnjspkdhojoigncdgmy.supabase.co
            NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJubmpzcGtkaG9qb2lnbmNkZ215Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODg4MTQ3NTEsImV4cCI6MjEwNDM5MDc1MX0.AQB1LBmLVuvMFT66ez-9LTLj5R8jofXhc5GGR8tKsBY
            NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_g_qPBuwhIOs3Z3GJ66OZnw_ZgUvxWKP
          tags: ghcr.io/ceesam111/esut-smart-library:latest
